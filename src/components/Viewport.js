const Viewport = ({children}) =>{
    return (
        <div className="Viewport">
            <div className="container">
                {children}
            </div>
        </div>
    )
}
export default Viewport